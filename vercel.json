{
  "buildCommand": "npm install --legacy-peer-deps && npm install typescript@5.8.2 --save-dev && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ]
} 